apiVersion: v1
kind: Service
metadata:
  name: fashion-payment
  namespace: fashion
  labels:
    app: fashion-payment
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-payment
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
