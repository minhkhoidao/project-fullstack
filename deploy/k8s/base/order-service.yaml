apiVersion: v1
kind: Service
metadata:
  name: fashion-order
  namespace: fashion
  labels:
    app: fashion-order
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-order
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
