apiVersion: v1
kind: Service
metadata:
  name: fashion-review
  namespace: fashion
  labels:
    app: fashion-review
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-review
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
