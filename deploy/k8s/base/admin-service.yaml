apiVersion: v1
kind: Service
metadata:
  name: fashion-admin
  namespace: fashion
  labels:
    app: fashion-admin
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-admin
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
