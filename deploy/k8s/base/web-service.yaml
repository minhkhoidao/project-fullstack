apiVersion: v1
kind: Service
metadata:
  name: fashion-web
  namespace: fashion
  labels:
    app: fashion-web
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-web
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
