apiVersion: v1
kind: Service
metadata:
  name: fashion-cart
  namespace: fashion
  labels:
    app: fashion-cart
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-cart
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
