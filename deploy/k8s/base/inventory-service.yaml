apiVersion: v1
kind: Service
metadata:
  name: fashion-inventory
  namespace: fashion
  labels:
    app: fashion-inventory
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-inventory
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
