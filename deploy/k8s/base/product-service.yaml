apiVersion: v1
kind: Service
metadata:
  name: fashion-product
  namespace: fashion
  labels:
    app: fashion-product
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-product
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
