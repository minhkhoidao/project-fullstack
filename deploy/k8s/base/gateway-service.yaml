apiVersion: v1
kind: Service
metadata:
  name: fashion-gateway
  namespace: fashion
  labels:
    app: fashion-gateway
    app.kubernetes.io/part-of: fashion-ecommerce
spec:
  type: ClusterIP
  selector:
    app: fashion-gateway
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
